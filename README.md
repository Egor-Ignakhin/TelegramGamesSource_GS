# Promo Hub - Лендинг промо игр

Современный лендинг для промо игр с удобным конфигом.

## 🚀 Быстрый старт

Откройте файл `index.html` в браузере или запустите локальный сервер:

```bash
python3 -m http.server 8080
```

Затем откройте в браузере: `http://localhost:8080`

## ⚙️ Как изменить игры

Все настройки находятся в файле **`config.js`**. Просто откройте его и измените что нужно!

### Изменить заголовок и тексты

```javascript
const PROMO_CONFIG = {
    title: 'Promo Hub',              // Главный заголовок
    subtitle: '3 увлекательные игры доступны',  // Подзаголовок
    footer: 'Промо игры для вашего бизнеса',    // Текст внизу
    games: [...]
};
```

### Добавить новую игру

Добавьте новый объект в массив `games`:

```javascript
games: [
    // ... существующие игры
    {
        id: 'mygame',                    // Уникальный ID
        name: 'Моя новая игра',          // Название
        color: '#FF6B9D',                // Цвет карточки (HEX)
        icon: 'blocks',                  // Иконка: brain, blocks, grid
        url: 'https://example.com/game'  // Ссылка на игру
    },
]
```

### Изменить существующую игру

Просто найдите нужную игру в массиве и измените параметры:

```javascript
{
    id: 'memory',
    name: 'Супер Мемори',           // Новое название
    color: '#FF5722',               // Новый цвет
    icon: 'brain',
    url: 'https://мойсайт.com/memory'  // Новая ссылка
},
```

### Удалить игру

Просто удалите или закомментируйте нужный блок:

```javascript
// {
//     id: 'tetris',
//     name: 'Тетрис',
//     ...
// },
```

## 🎨 Доступные иконки

- `brain` - мозг (для игр на память)
- `blocks` - блоки (для головоломок)
- `grid` - сетка (для match-3)

## 📁 Структура проекта

```
/
├── index.html      # HTML страница
├── styles.css      # Стили и дизайн
├── script.js       # Логика работы
├── config.js       # ⭐ КОНФИГ (редактируй здесь!)
└── README.md       # Эта инструкция
```

## 💡 Примеры

### Открывать игры в новой вкладке

В файле `script.js` замените строку:

```javascript
window.location.href = game.url;
```

На:

```javascript
window.open(game.url, '_blank');
```

### Изменить количество игр в подзаголовке

Подзаголовок автоматически берется из конфига, просто измените:

```javascript
subtitle: '5 супер игр доступны',  // Любой текст
```

### Изменить цвет фона

В файле `styles.css` найдите:

```css
:root {
    --bg-gradient-start: #4A9FD4;
    --bg-gradient-end: #2E7AB8;
}
```

И измените цвета!

## 🎮 Готово!

Теперь вы можете легко управлять своими играми через `config.js`!

